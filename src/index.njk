<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" rx="14" fill="#12141C"/>
  <text x="50%" y="58%" text-anchor="middle" font-family="monospace" font-size="30" font-weight="700" fill="#F2A93B">+P</text>
</svg>

---
permalink: /feed.xml
eleventyExcludeFromCollections: true
---<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title }}</title>
  <subtitle>{{ site.description }}</subtitle>
  <link href="{{ site.url }}/feed.xml" rel="self"/>
  <link href="{{ site.url }}/"/>
  <updated>{{ collections.allPosts[0].date | htmlDateString }}T00:00:00Z</updated>
  <id>{{ site.url }}/</id>
  <author><name>{{ site.author }}</name></author>
  {% for post in collections.allPosts | limit(20) %}
  <entry>
    <title>{{ post.data.title }}</title>
    <link href="{{ site.url }}{{ post.url }}"/>
    <updated>{{ post.date | htmlDateString }}T00:00:00Z</updated>
    <id>{{ site.url }}{{ post.url }}</id>
    <summary>{{ post.data.dek or (post.templateContent | excerpt(200)) }}</summary>
  </entry>
  {% endfor %}
</feed>

---
layout: layouts/base.njk
title: null
---
<section class="hero">
  <div class="wrap">
    <div class="hero-eyebrow mono">LIVE — SHIPPING NEW PATCHES DAILY</div>
    <h1 class="hero-title">Gaming news, reviews, and hot takes — pushed daily.</h1>
    <p class="hero-sub">{{ site.description }}</p>
  </div>
</section>

{% if collections.featured.length %}
{% set feat = collections.featured[0] %}
<section class="wrap">
  <div class="featured">
    <div class="featured-media">
      {% if feat.data.image %}<img src="{{ feat.data.image }}" alt="{{ feat.data.title }}">{% endif %}
    </div>
    <div>
      <span class="badge badge-{{ feat.data.category }}">{{ feat.data.category }}</span>
      <h2><a href="{{ feat.url }}">{{ feat.data.title }}</a></h2>
      <p class="excerpt">{{ feat.data.dek or (feat.templateContent | excerpt(180)) }}</p>
      <span class="mono" style="color:var(--paper-dim); font-size:0.8rem;">{{ feat.date | versionTag }} · {{ feat.date | readableDate }}</span>
    </div>
  </div>
</section>
{% endif %}

<section class="section">
  <div class="wrap">
    <div class="section-head">
      <h2><span style="color:var(--add);">+</span> Latest News</h2>
      <a class="see-all" href="/news/">view all →</a>
    </div>
    <div class="grid">
      {% for post in collections.news | limit(3) %}
      {% include "partials/card.njk" %}
      {% endfor %}
    </div>
  </div>
</section>

<section class="section">
  <div class="wrap">
    {% include "partials/ad-slot.njk" %}
  </div>
</section>

<section class="section">
  <div class="wrap">
    <div class="section-head">
      <h2><span style="color:var(--amber);">*</span> Fresh Reviews</h2>
      <a class="see-all" href="/reviews/">view all →</a>
    </div>
    <div class="grid">
      {% for post in collections.reviews | limit(3) %}
      {% include "partials/card.njk" %}
      {% endfor %}
    </div>
  </div>
</section>

<section class="section" style="border-bottom:none;">
  <div class="wrap">
    <div class="section-head">
      <h2><span style="color:var(--violet);">~</span> Opinions & Hot Takes</h2>
      <a class="see-all" href="/opinions/">view all →</a>
    </div>
    <div class="grid">
      {% for post in collections.opinions | limit(3) %}
      {% include "partials/card.njk" %}
      {% endfor %}
    </div>
  </div>
</section>

---
layout: layouts/base.njk
title: News
description: The latest gaming news, patch breakdowns, and industry updates from The Patch.
category: news
pagination:
  data: collections.news
  size: 12
  alias: posts
permalink: "/news/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber }}/{% endif %}"
---
<header class="page-header">
  <div class="wrap">
    <span class="badge badge-news">news</span>
    <h1>News</h1>
    <p style="color:var(--paper-dim);">Patch breakdowns, release dates, industry moves — as they drop.</p>
  </div>
</header>

<section class="section" style="border-bottom:none;">
  <div class="wrap">
    <div class="grid">
      {% for post in posts %}
      {% include "partials/card.njk" %}
      {% endfor %}
    </div>
  </div>
</section>

---
layout: layouts/base.njk
title: Opinions
description: Hot takes, columns, and analysis on the gaming industry from The Patch.
category: opinions
pagination:
  data: collections.opinions
  size: 12
  alias: posts
permalink: "/opinions/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber }}/{% endif %}"
---
<header class="page-header">
  <div class="wrap">
    <span class="badge badge-opinions">opinions</span>
    <h1>Opinions</h1>
    <p style="color:var(--paper-dim);">Columns and hot takes on where gaming is headed — argue with us on X.</p>
  </div>
</header>

<section class="section" style="border-bottom:none;">
  <div class="wrap">
    <div class="grid">
      {% for post in posts %}
      {% include "partials/card.njk" %}
      {% endfor %}
    </div>
  </div>
</section>

---
layout: layouts/page.njk
title: Privacy Policy
description: How The Patch handles data, cookies, and advertising.
---

*Last updated: July 2026*

> This is a starting template, not legal advice. Swap in your real business name and contact details, and consider having a lawyer (or a service like Termly / iubenda) review it before you rely on it, especially if you'll have EU or California visitors.

## What we collect

The Patch collects standard analytics data (pages visited, approximate location, device/browser type) to understand what content is working. We don't require an account to read the site.

## Cookies and advertising

This site uses cookies and may show ads served by Google AdSense and its partners. Google and its partners use cookies to serve ads based on your prior visits to this and other websites.

- You can opt out of personalized advertising by visiting [Google's Ads Settings](https://adssettings.google.com/).
- You can opt out of third-party vendor use of cookies via the [Digital Advertising Alliance's opt-out page](https://optout.aboutads.info/).
- For EU/UK visitors, more detail on Google's approach is available at [Google's Partner Sites policy](https://policies.google.com/technologies/partner-sites).

The banner shown on this site lets you accept or decline non-essential cookies.

## Third-party links

Articles may link to external sites (stores, publisher pages, social platforms). We aren't responsible for the privacy practices of those sites.

## Children's privacy

This site is not directed at children under 13, and we don't knowingly collect personal information from them.

## Contact

Questions about this policy: replace-this@example.com

**Edit this page** at `src/privacy-policy.md`.

---
layout: layouts/base.njk
title: Reviews
description: In-depth game reviews with clear verdicts from The Patch.
category: reviews
pagination:
  data: collections.reviews
  size: 12
  alias: posts
permalink: "/reviews/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber }}/{% endif %}"
---
<header class="page-header">
  <div class="wrap">
    <span class="badge badge-reviews">reviews</span>
    <h1>Reviews</h1>
    <p style="color:var(--paper-dim);">Every review ships with a Verdict Patch — a clear score and a plain-English breakdown of what's good and what's not.</p>
  </div>
</header>

<section class="section" style="border-bottom:none;">
  <div class="wrap">
    <div class="grid">
      {% for post in posts %}
      {% include "partials/card.njk" %}
      {% endfor %}
    </div>
  </div>
</section>

User-agent: *
Allow: /
Disallow: /admin/

Sitemap: https://thepatch.example.com/sitemap.xml

---
permalink: /sitemap.xml
eleventyExcludeFromCollections: true
---<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ site.url }}/</loc></url>
  <url><loc>{{ site.url }}/news/</loc></url>
  <url><loc>{{ site.url }}/reviews/</loc></url>
  <url><loc>{{ site.url }}/opinions/</loc></url>
  <url><loc>{{ site.url }}/about/</loc></url>
  <url><loc>{{ site.url }}/contact/</loc></url>
  <url><loc>{{ site.url }}/privacy-policy/</loc></url>
  {% for post in collections.allPosts %}
  <url>
    <loc>{{ site.url }}{{ post.url }}</loc>
    <lastmod>{{ post.date | htmlDateString }}</lastmod>
  </url>
  {% endfor %}
</urlset>

/admin/*  /admin/index.html  200

---
layout: layouts/page.njk
title: About The Patch
description: What The Patch is and who's behind it.
---

The Patch covers gaming news, reviews, and opinion pieces — written like patch notes because that's the fastest way to tell you what actually changed.

We're independent and not affiliated with any publisher or platform holder. When we review something, we say what we actually think.

**Edit this page** at `src/about.md` (or in the admin panel under "Pages") to add your own bio, mission, and links to your socials.

# Replace pub-0000000000000000 with your real AdSense publisher ID after you're approved.
# Find it in AdSense under Account > Account information.
google.com, pub-0000000000000000, DIRECT, f08c47fec0942fa0

---
layout: layouts/page.njk
title: Contact
description: Get in touch with The Patch.
---

Got a tip, a review copy to send, or a correction? Reach out:

- **Email:** replace-this@example.com
- **X:** [@thepatchgg](https://x.com/thepatchgg)
- **TikTok:** [@thepatchgg](https://tiktok.com/@thepatchgg)

**Edit this page** at `src/contact.md` to swap in your real email address and handles once they're live.
